---
description: "Notification Layer Team Orchestrator — diagnoses and fixes sound alerts (Howler.js), color/glow animations (CSS + useSettledStatus), and TTS voice (Piper + AudioContext). Knows the two-path architecture and every related commit."
---

Read and execute the team orchestrator skill definition.

Skill location (relative to agenthub root): `plugin/skills/team-notification-layer/SKILL.md`

If you are NOT working in the agenthub repo, use the AGENTHUB_HOME environment variable
to resolve the full path. Read the SKILL.md file and follow its instructions exactly.
