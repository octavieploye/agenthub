# package-ts-factory — criteria

# Default output directory for generated packages (relative to project root)
PACKAGES_DIR=packages

# Reference pattern (read-only — do not modify)
TS_PATTERN=destructuring

# Default dependency versions
ZOD_VERSION=^4.3.6
VITEST_VERSION=^2.0.0
TYPESCRIPT_VERSION=^5.5.0

# Target: llm-workflows-pckg (Opeidos)
TARGET_REPO=llm-workflows-pckg
