# package-factory — criteria

# Default output directory for generated packages (relative to project root)
PACKAGES_DIR=packages

# Default package type if not specified
DEFAULT_TYPE=workflow

# Reference patterns (read-only — do not modify)
WORKFLOW_PATTERN=market-modeling
TOOL_PATTERN=token-optimizer
